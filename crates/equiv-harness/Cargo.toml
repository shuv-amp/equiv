[package]
name = "equiv-harness"
description = "Generates a differential probe that calls two versions of a function and compares them."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
equiv-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
syn = { workspace = true }
sha2 = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
